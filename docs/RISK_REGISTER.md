# 风险登记表

| 风险 | 当前状态 | 缓解措施 | 未完成证据 |
|---|---|---|---|
| GMM 样本内后验过度自信 | 已确认 | 临时簇名、监督留出验证、独立概率校准 | 尚无 2,000–5,000 段人工真值 |
| HDBSCAN 可选依赖缺失可能被误读为“零噪声” | 已修正并加回归测试 | 当前运行时未安装 `hdbscan`；逐行 `hdbscan_available=false` 且 `hdbscan_noise=NA`，不得把缺少模型当作全样本通过密度门控 | 需在安装 optional discovery 依赖的环境单独验证 HDBSCAN 全链路；Student-t 尚未集成到当前发现器 |
| 发现器混合模型未收敛仍可输出临时簇 | 已修正并加回归测试，且在较大样本验证门控 | 任一 GMM bootstrap 成员或 DPGMM 未收敛时记录 `mixture_nonconverged` 并把该版本预测门控为 UNKNOWN；64 股因果样本在 `max_iter=100` 有 7/34 版本未收敛（1,490 行全弃权），同一冻结特征将上限调至 200 后 34/34 收敛，覆盖率由 36.7% 升至 47.0% | 迭代数提高只改善收敛/输出覆盖，不代表分类语义或正确率提升；正式选择仍需人工真值与校准集验证 |
| 训练股票组成变化会重塑临时簇 | 已量化，风险较高 | 2015–2024 64 股样本的 34 个因果折做 5 次按 symbol 整簇重采样/折，基线完全复现；170 个重拟合模型收敛，但 OOS 已识别类别一致率仅 62.6%、软概率 TV 均值 0.304，B/C 临时簇一致率约 56.8%/54.1%；簇仅折内用训练中心匈牙利对齐 | 这是模型稳定性而非语义正确率；64 股样本非全市场代表，尚未做年份/制度分块重采样；均值中心对齐在重叠簇时可能含歧义，未有人工标签前禁止解释为业务类别 |
| 全市场重采样稳定率可能被 transductive 口径误读 | 已新增 341,544 段内部压力测试 | 重建历史离线模型训练 ID 哈希一致、341,544 个已保存标签精确复现；3 次按 symbol 整簇重采样后，已识别类一致率 91.6%，临时簇按共同训练软概率重叠对齐 | 全历史训练并回看全历史候选，不是时间外/OOS；不能与 64 股因果折稳定性横向比较或称为分类准确率。重采样仅 3 次，未按年份/市场制度抽块；簇 E 一致率 81.3%，UNKNOWN 约 22.6% 会改变为已识别。HDBSCAN 未安装/未测 |
| 跨年份/牛熊切片可能被样本支持与代理定义误导 | 已初步切片、证据有限 | 以 `available_at` 年份切片折内稳定度；另用 64 股每日横截面中位收益、过去 20 日累计 ±5% 定义 `BULL_PROXY/BEAR_PROXY/SIDEWAYS_PROXY`，不使用未来值 | 2016 年基线全 UNKNOWN；牛市代理仅 360 个可评分段；没有官方宽基指数、全市场指数权重或市场制度标签。上述切片是 64 股内部探索结果，不能外推市场状态精度或跨阶段显著性 |
| 临时发现簇视觉上仍可能混合多种价格路径 | 已观察并保持非语义命名 | 10 段探索性视觉试审中，CLUSTER_B/C/E 各见不同路径；继续保留 `CLUSTER_*` 与 UNKNOWN，不据此赋业务语义 | 每类仅 2 个主观样本，无人工真值/类内一致性证据，不能用于语义命名或分类准确率声明 |
| 边界参数敏感和过分割 | 已确认并初步缓解 | bootstrap 稳定频率、容差 F1、分形/BOCPD、多方法融合、HSMM、连续同类合并 | 尚无大规模人工边界真值 |
| 全市场候选构建的 bootstrap 参数此前未实际计算稳定度 | 已修正并加回归测试 | `bootstrap_iterations>0` 时按股票分组扰动，边界概率取检测器概率与 bootstrap 频率的较小值；默认 0 以控制全市场成本 | 全市场完整高迭代 bootstrap 尚未运行；稳定度不是真实边界准确率，仍缺人工边界真值 |
| 合并同类段后边界稳定度可能沿用内部旧端点 | 已修正并加回归测试 | 保留两侧端点的 detector/bootstrap 证据；合并时取首段起点和末段终点，重算综合概率，内部被消除的边界不污染结果 | 需在启用 bootstrap 的真实多股票输出上复核；聚合边界证据仍不是人工正确率 |
| 离线 HSMM 未来泄露 | 已隔离 | 历史报告用 `hsmm_label`；选股仅用 `causal_duration_label` | 需审计下游字段白名单 |
| 高 UNKNOWN 占比下转移率缺少足够分母 | 已记录 | 同时报告 UNKNOWN 占比和相邻已识别转移对数；小分母只作描述，不解释为过分割已改善/恶化 | 需在覆盖更充分且有人工边界/类别真值的样本上复核 |
| 人工语义误命名 | 已缓解 | 默认 `CLUSTER_*`，语义仅作建议 | 等待多人审核命名 |
| 发现簇的跨股票/跨年份支持可能被缺失元数据虚假满足 | 已修正并加回归测试 | `symbol` 或 `start` 缺失时相应支持门槛 fail-closed，候选簇不足支持则输出 `UNKNOWN` | 需在真实全市场候选表上审计元数据缺失率及影响簇数；无人工真值前不代表语义正确 |
| 人工审核选择偏差 | 代码防护 | UNKNOWN 富集 + 年份/股票/行业/状态分层随机抽样，保存入样概率 | 尚无 2,000–5,000 段实际多人审核结果 |
| 未分类候选与模型弃权共享 UNKNOWN 展示值 | 已修正并加回归测试 | 审核队列新增 `candidate_status` 区分 `unreviewed_candidate`、`model_abstention`、`classified`；UI 明示语义并在审核事件中留痕 | 旧队列仍需重建才能显示该字段；不得把全 UNKNOWN 的候选队列解读为模型拒识率 |
| 分类数据泄露 | 代码防护 | 全市场统一日历 Purged Walk-forward、train/cal/test 全局区间隔离、禁止未来字段 | 需在真实人工标签集上审计每个 fold |
| 选股层未来泄露 | 代码防护 | backward as-of、显式 shift、模块隔离 | 财报发布日期/历史行业数据尚未接入 |
| 幸存者偏差 | 部分缓解 | 时点股票池含退市股 | 尚未下载全市场历史股票池快照 |
| 复权/公司行动异常 | 未完全验证 | 前复权、分区缓存、异常跳跃审核 | 长区间 Tushare 复权接口速度不稳定 |
| 网络请求伪超时与线程泄漏 | 已修正 | 不用线程取消模拟硬超时；在 Tushare HTTP 客户端设置 timeout，逐日原子分区可恢复 | 长周期下载仍需外部进程监督与速率治理 |
| 长周期下载“假完整” | 核心行情通过；选股元数据仍不完整 | 固化交易日清单；可要求指定元数据端点逐日齐备并审计截断、分区、行数、重复键、复权匹配 | 2026-09-17 发现旧审计默认只要求核心行情；核心覆盖 2,431 日/9,402,259 行，adj_factor 匹配率 100%。`daily_basic`、`stk_limit`、`suspend_d` 目前各 1,095/2,431 日齐备；严格审计报告 `data/a_share_2015_2024/bulk_core_audit_metadata_progress_20260917.json`，尚有每端点 1,336 日缺口。历史股票池偏差和源数据正确性仍未证明 |
| 人工基准集尚未启动 | 未完成（正式真值 0） | 从分层候选中进行双人独立试标；UNKNOWN/疑难段优先，同时保留代表性随机样本；检查边界和标签一致性后再扩大 | `outputs/fullmarket_candidates_hfq/annotation_sample_3000.csv` 当前 3,000 行均未审核；64 股因果样本的 20 段 AI 视觉试审记录于 `outputs/fullmarket_causal_64_sample_2015_2024_discovery_iter200/visual_review_pilot_20/AI_VISUAL_REVIEW.md`，不是人工真值、不能用于一致性/校准/测试 |
| 停牌/涨跌停与可交易性 | 初步实现（严格筛选） | 按交易日精确接入 `suspend_d`/`stk_limit`；缺来源默认不可用；第二层自动消费 `selection_eligible` 并检查目标退出日可交易性 | 缺全市场历史状态数据、盘中成交数量与排队成交规则 |
| 历史可交易性与行业归属错配 | 初步缓解 | `market_data.enrich_and_audit_market_data` 仅按股票/交易日精确连接 daily_basic、涨跌停和停牌；申万行业只在 in/out 生效区间内连接，并输出覆盖率、缺失和历史股票池完整度 | 尚未下载全市场的 daily_basic、stk_limit、suspend_d 和申万历史成员快照；数据缺失不得当作可交易或行业中性 |
| 元数据发布日期/修订泄露 | 代码防护 | 逐日元数据不 forward-fill；行业按有效区间；财报使用 `max(ann_date,f_ann_date)+lag` 后才可见，同报告期修订仅从修订日生效 | 尚需下载全市场财报历史并核验供应商字段语义与公告时间粒度 |
| 成本与滑点低估 | 初版 | 显式 bps 成本与换手扣减 | 缺成交额/冲击成本模型 |
| Conformal 分布漂移 | 已记录 | 按市场制度重校准、监控覆盖 | 尚无跨周期数据验证 |
| 波段发现的“事后确认”泄露 | 已实现并审计小样本因果状态链 | `segment_ohlcv_causal` 只在 ATR 反转阈值触发时确认 pivot，分别记录 pivot `end` 与 `available_at`；遗漏未闭合尾段；walk-forward 强制要求显式可用时点，并只 purge 同一证券内有 K 线区间重叠的样本。因果 bootstrap 每次运行在线分割并按每段确认索引过滤，prefix-invariance 有回归测试。8 股试点清单通过时间先后、purge、概率归一和 UNKNOWN 一致性检查 | 仅 8 股；因果 bootstrap 的20轮均值覆盖度仍不是边界真值；2021年因跨年支持覆盖0%；单一 ATR-ZigZag；簇 ID 仅 fold-local；离线融合候选及当前试点均不可用于选股 |
| 全历史 bootstrap 将未来信息混入早期边界稳定度 | 已隔离并修复 | `outputs/causal_pilot_8_2021_2024_v3/manifest.json` 标记 `invalidated_for_causal_use`；新 `bootstrap_causal_boundary_stability` 只计入确认索引不晚于被评边界确认时点的扰动候选，配置与稳定度结果写入新试点 manifest | v3 的 OOS 状态不得引用；全市场因果 bootstrap 尚未执行，前缀不变性保证仍依赖当前因果分割器 |

任何新增风险先加入本表，再继续扩大模型复杂度。当前产品仍是波段样本发现器和审核平台，不能用于自动化选股。
